// AUTO-GENERATED FILE. DO NOT EDIT DIRECTLY.
// Source: src/plugins/modules/shop/prisma/schema.prisma
// Shop 电商模块 - 完整 SKU 体系

// ===================== 商品分类 =====================

model ShopCategory {
  id          Int       @id @default(autoincrement()) @map("id")
  name        String    @map("name") @db.VarChar(100)
  parentId    Int?      @map("parent_id")
  coverImage  String?   @map("cover_image") @db.VarChar(500)
  icon        String?   @map("icon") @db.VarChar(100)
  description String?   @map("description") @db.VarChar(500)
  sortOrder   Int       @default(0) @map("sort_order")
  status      Int       @default(1) @map("status") @db.TinyInt
  creatorId   Int       @map("creator_id")
  createdAt   DateTime  @default(now()) @map("created_at") @db.DateTime(0)
  updaterId   Int       @map("updater_id")
  updatedAt   DateTime  @default(now()) @updatedAt @map("updated_at") @db.DateTime(0)
  deletedAt   DateTime? @map("deleted_at") @db.DateTime(0)
  version     Int       @default(1) @map("version")

  parent   ShopCategory?  @relation("ShopCategoryParent", fields: [parentId], references: [id], onUpdate: NoAction, onDelete: NoAction)
  children ShopCategory[] @relation("ShopCategoryParent")
  products ShopProduct[]

  @@index([status], map: "idx_shop_category_status")
  @@index([parentId], map: "idx_shop_category_parent_id")
  @@index([sortOrder], map: "idx_shop_category_sort_order")
  @@index([deletedAt], map: "idx_shop_category_deleted_at")
  @@map("shop_category")
}

// ===================== 商品属性定义 =====================
model ShopAttribute {
  id        Int       @id @default(autoincrement()) @map("id")
  name      String    @map("name") @db.VarChar(50)
  type      Int       @default(1) @map("type") @db.TinyInt
  sortOrder Int       @default(0) @map("sort_order")
  status    Int       @default(1) @map("status") @db.TinyInt
  creatorId Int       @map("creator_id")
  createdAt DateTime  @default(now()) @map("created_at") @db.DateTime(0)
  updaterId Int       @map("updater_id")
  updatedAt DateTime  @default(now()) @updatedAt @map("updated_at") @db.DateTime(0)
  deletedAt DateTime? @map("deleted_at") @db.DateTime(0)
  version   Int       @default(1) @map("version")

  values ShopAttributeValue[]
  skus   ShopSkuAttribute[]

  @@index([status], map: "idx_shop_attribute_status")
  @@index([type], map: "idx_shop_attribute_type")
  @@index([deletedAt], map: "idx_shop_attribute_deleted_at")
  @@map("shop_attribute")
}

// ===================== 属性值 =====================
model ShopAttributeValue {
  id          Int       @id @default(autoincrement()) @map("id")
  attributeId Int       @map("attribute_id")
  value       String    @map("value") @db.VarChar(100)
  image       String?   @map("image") @db.VarChar(500)
  sortOrder   Int       @default(0) @map("sort_order")
  status      Int       @default(1) @map("status") @db.TinyInt
  creatorId   Int       @map("creator_id")
  createdAt   DateTime  @default(now()) @map("created_at") @db.DateTime(0)
  updaterId   Int       @map("updater_id")
  updatedAt   DateTime  @default(now()) @updatedAt @map("updated_at") @db.DateTime(0)
  deletedAt   DateTime? @map("deleted_at") @db.DateTime(0)
  version     Int       @default(1) @map("version")

  attribute ShopAttribute      @relation(fields: [attributeId], references: [id], onDelete: Cascade)
  skuValues ShopSkuAttribute[]

  @@index([attributeId], map: "idx_shop_attr_value_attr_id")
  @@index([status], map: "idx_shop_attr_value_status")
  @@index([deletedAt], map: "idx_shop_attr_value_deleted_at")
  @@map("shop_attribute_value")
}

// ===================== 商品主表 =====================
model ShopProduct {
  id          Int       @id @default(autoincrement()) @map("id")
  categoryId  Int       @map("category_id")
  name        String    @map("name") @db.VarChar(200)
  subtitle    String?   @map("subtitle") @db.VarChar(500)
  coverImage  String?   @map("cover_image") @db.VarChar(500)
  images      Json?     @map("images")
  description String?   @map("description") @db.Text
  price       Decimal   @map("price") @db.Decimal(10, 2)
  costPrice   Decimal?  @map("cost_price") @db.Decimal(10, 2)
  stock       Int       @default(0) @map("stock")
  unit        String    @default("件") @map("unit") @db.VarChar(20)
  weight      Decimal?  @map("weight") @db.Decimal(10, 2)
  status      Int       @default(1) @map("status") @db.TinyInt
  isHot       Boolean   @default(false) @map("is_hot")
  isNew       Boolean   @default(false) @map("is_new")
  isRecycle   Boolean   @default(false) @map("is_recycle")
  sortOrder   Int       @default(0) @map("sort_order")
  clickCount  Int       @default(0) @map("click_count")
  creatorId   Int       @map("creator_id")
  createdAt   DateTime  @default(now()) @map("created_at") @db.DateTime(0)
  updaterId   Int       @map("updater_id")
  updatedAt   DateTime  @default(now()) @updatedAt @map("updated_at") @db.DateTime(0)
  deletedAt   DateTime? @map("deleted_at") @db.DateTime(0)
  version     Int       @default(1) @map("version")

  category   ShopCategory    @relation(fields: [categoryId], references: [id], onUpdate: NoAction, onDelete: NoAction)
  skus       ShopSku[]
  cartItems  ShopCart[]
  orderItems ShopOrderItem[]

  @@index([categoryId], map: "idx_shop_product_category")
  @@index([status], map: "idx_shop_product_status")
  @@index([isHot, status], map: "idx_shop_product_hot")
  @@index([isNew, status], map: "idx_shop_product_new")
  @@index([isRecycle], map: "idx_shop_product_recycle")
  @@index([price], map: "idx_shop_product_price")
  @@index([createdAt], map: "idx_shop_product_created")
  @@index([deletedAt], map: "idx_shop_product_deleted_at")
  @@map("shop_product")
}

// ===================== SKU 表 =====================
model ShopSku {
  id         Int       @id @default(autoincrement()) @map("id")
  productId  Int       @map("product_id")
  skuCode    String    @unique @map("sku_code") @db.VarChar(64)
  skuName    String    @map("sku_name") @db.VarChar(500)
  price      Decimal   @map("price") @db.Decimal(10, 2)
  costPrice  Decimal?  @map("cost_price") @db.Decimal(10, 2)
  stock      Int       @default(0) @map("stock")
  weight     Decimal?  @map("weight") @db.Decimal(10, 2)
  coverImage String?   @map("cover_image") @db.VarChar(500)
  status     Int       @default(1) @map("status") @db.TinyInt
  creatorId  Int       @map("creator_id")
  createdAt  DateTime  @default(now()) @map("created_at") @db.DateTime(0)
  updaterId  Int       @map("updater_id")
  updatedAt  DateTime  @default(now()) @updatedAt @map("updated_at") @db.DateTime(0)
  deletedAt  DateTime? @map("deleted_at") @db.DateTime(0)
  version    Int       @default(1) @map("version")

  product    ShopProduct        @relation(fields: [productId], references: [id], onDelete: Cascade)
  skuValues  ShopSkuAttribute[]
  cartItems  ShopCart[]
  orderItems ShopOrderItem[]

  @@index([productId], map: "idx_shop_sku_product")
  @@index([skuCode], map: "idx_shop_sku_code")
  @@index([status], map: "idx_shop_sku_status")
  @@index([deletedAt], map: "idx_shop_sku_deleted_at")
  @@map("shop_product_sku")
}

// ===================== SKU 属性关联 =====================
model ShopSkuAttribute {
  id          Int @id @default(autoincrement()) @map("id")
  skuId       Int @map("sku_id")
  attributeId Int @map("attribute_id")
  valueId     Int @map("value_id")

  sku       ShopSku            @relation(fields: [skuId], references: [id], onDelete: Cascade)
  attribute ShopAttribute      @relation(fields: [attributeId], references: [id], onUpdate: NoAction, onDelete: NoAction)
  value     ShopAttributeValue @relation(fields: [valueId], references: [id], onUpdate: NoAction, onDelete: NoAction)

  @@unique([skuId, attributeId, valueId], map: "uniq_sku_attr_value")
  @@index([skuId], map: "idx_shop_sku_attr_sku")
  @@index([attributeId], map: "idx_shop_sku_attr_attr")
  @@index([valueId], map: "idx_shop_sku_attr_value")
  @@map("shop_sku_attribute")
}

// ===================== 会员收货地址 =====================
model ShopAddress {
  id        Int       @id @default(autoincrement()) @map("id")
  userId    Int       @map("user_id")
  receiver  String    @map("receiver") @db.VarChar(50)
  phone     String    @map("phone") @db.VarChar(20)
  province  String    @map("province") @db.VarChar(50)
  city      String    @map("city") @db.VarChar(50)
  district  String    @map("district") @db.VarChar(50)
  address   String    @map("address") @db.VarChar(255)
  isDefault Boolean   @default(false) @map("is_default")
  status    Int       @default(1) @map("status") @db.TinyInt
  creatorId Int       @map("creator_id")
  createdAt DateTime  @default(now()) @map("created_at") @db.DateTime(0)
  updaterId Int       @map("updater_id")
  updatedAt DateTime  @default(now()) @updatedAt @map("updated_at") @db.DateTime(0)
  deletedAt DateTime? @map("deleted_at") @db.DateTime(0)
  version   Int       @default(1) @map("version")

  user   SysUser     @relation(fields: [userId], references: [id], onDelete: Cascade)
  orders ShopOrder[]

  @@index([userId], map: "idx_shop_address_user")
  @@index([isDefault], map: "idx_shop_address_default")
  @@index([status], map: "idx_shop_address_status")
  @@index([deletedAt], map: "idx_shop_address_deleted_at")
  @@map("shop_address")
}

// ===================== 购物车 =====================
model ShopCart {
  id        Int       @id @default(autoincrement()) @map("id")
  userId    Int       @map("user_id")
  productId Int       @map("product_id")
  skuId     Int?      @map("sku_id")
  quantity  Int       @default(1) @map("quantity")
  status    Int       @default(1) @map("status") @db.TinyInt
  creatorId Int       @map("creator_id")
  createdAt DateTime  @default(now()) @map("created_at") @db.DateTime(0)
  updaterId Int       @map("updater_id")
  updatedAt DateTime  @default(now()) @updatedAt @map("updated_at") @db.DateTime(0)
  deletedAt DateTime? @map("deleted_at") @db.DateTime(0)
  version   Int       @default(1) @map("version")

  user    SysUser     @relation(fields: [userId], references: [id], onDelete: Cascade)
  product ShopProduct @relation(fields: [productId], references: [id], onUpdate: NoAction, onDelete: NoAction)
  sku     ShopSku?    @relation(fields: [skuId], references: [id], onUpdate: NoAction, onDelete: SetNull)

  @@unique([userId, productId, skuId], map: "uniq_cart_user_product_sku")
  @@index([userId], map: "idx_shop_cart_user")
  @@index([status], map: "idx_shop_cart_status")
  @@index([deletedAt], map: "idx_shop_cart_deleted_at")
  @@map("shop_cart")
}

// ===================== 订单主表 =====================
model ShopOrder {
  id               Int       @id @default(autoincrement()) @map("id")
  orderNo          String    @unique @map("order_no") @db.VarChar(32)
  userId           Int       @map("user_id")
  addressId        Int       @map("address_id")
  totalAmount      Decimal   @map("total_amount") @db.Decimal(10, 2)
  freightAmount    Decimal   @default(0) @map("freight_amount") @db.Decimal(10, 2)
  discountAmount   Decimal   @default(0) @map("discount_amount") @db.Decimal(10, 2)
  payAmount        Decimal   @map("pay_amount") @db.Decimal(10, 2)
  payStatus        Int       @default(0) @map("pay_status") @db.TinyInt
  payTime          DateTime? @map("pay_time") @db.DateTime(0)
  payMethod        String?   @map("pay_method") @db.VarChar(20)
  payTransactionId String?   @map("pay_transaction_id") @db.VarChar(64)
  orderStatus      Int       @default(1) @map("order_status") @db.TinyInt
  expressCompany   String?   @map("express_company") @db.VarChar(50)
  expressNo        String?   @map("express_no") @db.VarChar(50)
  deliverTime      DateTime? @map("deliver_time") @db.DateTime(0)
  receiveTime      DateTime? @map("receive_time") @db.DateTime(0)
  cancelReason     String?   @map("cancel_reason") @db.VarChar(255)
  remark           String?   @map("remark") @db.VarChar(500)
  status           Int       @default(1) @map("status") @db.TinyInt
  creatorId        Int       @map("creator_id")
  createdAt        DateTime  @default(now()) @map("created_at") @db.DateTime(0)
  updaterId        Int       @map("updater_id")
  updatedAt        DateTime  @default(now()) @updatedAt @map("updated_at") @db.DateTime(0)
  deletedAt        DateTime? @map("deleted_at") @db.DateTime(0)
  version          Int       @default(1) @map("version")

  user    SysUser         @relation(fields: [userId], references: [id], onUpdate: NoAction, onDelete: NoAction)
  address ShopAddress     @relation(fields: [addressId], references: [id], onUpdate: NoAction, onDelete: NoAction)
  items   ShopOrderItem[]

  @@index([userId], map: "idx_shop_order_user")
  @@index([orderNo], map: "idx_shop_order_no")
  @@index([orderStatus], map: "idx_shop_order_status")
  @@index([payStatus], map: "idx_shop_order_pay_status")
  @@index([createdAt], map: "idx_shop_order_created")
  @@index([deletedAt], map: "idx_shop_order_deleted_at")
  @@index([expressNo], map: "idx_shop_order_express")
  @@map("shop_order")
}

// ===================== 订单商品明细 =====================
model ShopOrderItem {
  id          Int       @id @default(autoincrement()) @map("id")
  orderId     Int       @map("order_id")
  productId   Int       @map("product_id")
  skuId       Int?      @map("sku_id")
  skuName     String?   @map("sku_name") @db.VarChar(500)
  coverImage  String?   @map("cover_image") @db.VarChar(500)
  productName String    @map("product_name") @db.VarChar(200)
  price       Decimal   @map("price") @db.Decimal(10, 2)
  quantity    Int       @map("quantity")
  subtotal    Decimal   @map("subtotal") @db.Decimal(10, 2)
  status      Int       @default(1) @map("status") @db.TinyInt
  creatorId   Int       @map("creator_id")
  createdAt   DateTime  @default(now()) @map("created_at") @db.DateTime(0)
  updaterId   Int       @map("updater_id")
  updatedAt   DateTime  @default(now()) @updatedAt @map("updated_at") @db.DateTime(0)
  deletedAt   DateTime? @map("deleted_at") @db.DateTime(0)
  version     Int       @default(1) @map("version")

  order   ShopOrder   @relation(fields: [orderId], references: [id], onDelete: Cascade)
  product ShopProduct @relation(fields: [productId], references: [id], onUpdate: NoAction, onDelete: NoAction)
  sku     ShopSku?    @relation(fields: [skuId], references: [id], onUpdate: NoAction, onDelete: SetNull)

  @@index([orderId], map: "idx_shop_order_item_order")
  @@index([productId], map: "idx_shop_order_item_product")
  @@index([skuId], map: "idx_shop_order_item_sku")
  @@index([deletedAt], map: "idx_shop_order_item_deleted_at")
  @@map("shop_order_item")
}
