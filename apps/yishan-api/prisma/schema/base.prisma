// Base Prisma schema: generator & datasource

generator client {
  provider     = "prisma-client"
  output       = "../../src/generated/prisma"
  moduleFormat = "commonjs"
}

datasource db {
  provider     = "mysql"
  relationMode = "prisma"
}
